# PR #70 review with corrections

Reviewed remote head: 6dfa714c35258cd47746bb91b795092e958e25a7.
Reconciled with freshly fetched main: 0e9d5b0a1b718872ae41d008c99894a69aa349f7.
User explicitly requested review and correction on 2026-09-12.
Worktree: /tmp/beat-twin-pr70-review; local branch: review/pr-70-fixes.

## Findings resolved

1. P1 — The draft is based on dd61643 and conflicts with merged PRs #67/#68.
   Its runtime path, package dependencies and retention implementation no longer
   match main. Integrated main locally, preserved Node 26/authenticated provider
   support, moved V2 composition into apps/nanodaw-mcp, updated the script and
   test imports, and retained BoundedRetentionMap, injected clocks/stores and
   capacity reservations alongside the 32-plan pending inbox.
2. P1 — nanoDawProviderOptions discarded LITERT_API_KEY. Authenticated providers
   therefore rejected model discovery and chat even with a configured key.
   Forwarded the optional key and exercised the environment-to-runtime path
   with a synthetic fetch asserting Authorization on both model requests.
3. P2 — Inbox validation coerced instrumentId with String(), accepting arrays
   such as ["bass"] despite the advertised enum contract. Require a string
   before checking the enum; regression coverage rejects that malformed input.

## Verification

- pnpm test: 236 passed, zero failures.
- pnpm nanodaw:test: 166 passed across 17 files.
- Final focused agentGateway test run after validation correction: 5 passed.
- pnpm check:architecture: 16 workspaces, 43 internal runtime edges, no violations.
- Package smoke and node --check index.js passed.
- pnpm build: package/app TypeScript builds, playground tsc -b and Vite passed.
- git diff --check: passed.

The first sandboxed backend run failed because loopback listeners returned
EPERM. The authorized run outside the sandbox passed. All model responses and
DAW/browser state used in these tests are synthetic; no live model or DAW
was contacted.

## Adversarial review and remaining evidence

Reviewed V1/V2 selection and strict patch validation, fixed NanoDAW target,
proposal-only model tools, inbox authentication/Origin/Host gating and bounds,
preview preservation, confirmation and single-use execution, stale revision
rejection and lost-reply behavior. Main's uncertain/partial execution retention
remains intact. React skill guided inspection of async polling and cleanup.
No additional confirmed defect remains from this review.

Rendered desktop/mobile TWIN behavior, a real provider proposal and human
listening acceptance remain unverified. Draft status is appropriate.
Corrections are local; no GitHub review/comment, push, merge to main,
deployment or branch deletion was performed.
