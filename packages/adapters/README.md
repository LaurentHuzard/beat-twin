# Beat Twin Adapters

Reserved for host integrations.

The existing Bitwig MCP server remains in `index.js` for compatibility in Sprint 0+1. A later migration can introduce `adapters/bitwig` only after the current MCP exports, CLI binary, and policy tests are protected by a focused compatibility pass.
