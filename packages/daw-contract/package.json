{
  "name": "@beat-twin/daw-contract",
  "version": "0.0.0",
  "private": true,
  "description": "Fail-closed DAW adapter contract and reusable conformance suite for Beat Twin.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "node --test test/*.test.ts"
  },
  "dependencies": {
    "@beat-twin/commands": "workspace:*"
  }
}
