{
  "name": "@beat-twin/gateway-core",
  "version": "0.0.0",
  "private": true,
  "description": "Fail-closed pairing, plan, confirmation, quota, and audit core for the Beat Twin Gateway.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "node --test test/*.test.ts"
  },
  "dependencies": {
    "@beat-twin/daw-contract": "workspace:*",
    "@beat-twin/retention": "workspace:*"
  }
}
