{
  "name": "@beat-twin/litert-provider",
  "version": "0.0.0",
  "private": true,
  "description": "Strict OpenAI-compatible LiteRT-LM provider and bounded SongPatch agent loop.",
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc -p tsconfig.build.json --noEmit"
  },
  "dependencies": {
    "@beat-twin/agent-contract": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
