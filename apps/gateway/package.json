{
  "name": "@beat-twin/gateway",
  "version": "0.0.0",
  "private": true,
  "description": "Loopback-only HTTP gateway for LiteRT-LM agent planning and explicit DAW execution.",
  "type": "module",
  "main": "./src/index.js",
  "exports": {
    ".": "./src/index.js"
  },
  "scripts": {
    "test": "corepack pnpm --filter @beat-twin/gateway-http build && node --test test/*.test.js"
  },
  "engines": {
    "node": "^22.0.0 || ^24.0.0"
  },
  "dependencies": {
    "@beat-twin/gateway-http": "workspace:*"
  },
  "devDependencies": {
    "@beat-twin/commands": "workspace:*",
    "@beat-twin/gateway-core": "workspace:*",
    "ws": "^8.21.0"
  }
}
